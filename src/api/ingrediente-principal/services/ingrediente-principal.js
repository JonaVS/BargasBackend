'use strict';

/**
 * ingrediente-principal service.
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::ingrediente-principal.ingrediente-principal');
